import { Coins } from 'lucide-react';
import type { CoinWallet } from '@/services/v2Service';

export function FECoinWallet({wallet,title='FE Coins'}:{wallet:CoinWallet;title?:string}){return <section className="card overflow-hidden"><div className="bg-gradient-to-br from-amber-400 to-orange-500 p-5 text-white"><div className="flex items-center gap-2 text-sm font-semibold"><Coins size={19}/>{title}</div><p className="mt-3 text-3xl font-bold">{wallet.balance.toFixed(2)} FE</p><p className="text-xs text-white/80">Available balance</p></div><div className="grid grid-cols-2 border-b border-gray-100 dark:border-gray-800"><Stat label="Earned" value={`${wallet.earned.toFixed(2)} FE`}/><Stat label="Redeemed" value={`${wallet.redeemed.toFixed(2)} FE`}/></div><div className="p-4"><h3 className="text-sm font-semibold">Recent FE transactions</h3>{wallet.transactions.length?<div className="mt-3 space-y-3">{wallet.transactions.map((item)=><div key={item.id} className="flex items-start justify-between gap-3 text-sm"><div><p className="font-medium">{item.description}</p><p className="text-xs text-gray-400">{new Date(item.createdAt).toLocaleDateString()}</p></div><span className={item.amount>=0?'font-semibold text-success-600':'font-semibold text-gray-700 dark:text-gray-300'}>{item.amount>=0?'+':''}{item.amount.toFixed(2)} FE</span></div>)}</div>:<div className="py-5 text-center"><p className="text-sm font-medium">No FE Coin activity yet.</p><p className="mt-1 text-xs text-gray-500">Complete eligible verified Founder.env transactions to earn rewards.</p></div>}</div></section>}
function Stat({label,value}:{label:string;value:string}){return <div className="p-3 text-center first:border-r first:border-gray-100 dark:first:border-gray-800"><p className="font-semibold">{value}</p><p className="text-xs text-gray-500">{label}</p></div>}
